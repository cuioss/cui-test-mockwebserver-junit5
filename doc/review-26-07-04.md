# Project Review & Fix Plan — 2026-07-04

Full-project analysis of `cui-test-mockwebserver-junit5` (main code, tests, documentation,
build/CI). This document contains **findings and a fix plan only** — no fixes are applied by it.
It is the working checklist for the follow-up fix sessions and is **removed once every finding
below is verified as fixed** (see [Final Verification](#final-verification--document-removal)).

**Baseline at time of review** (commit `9f8cd77`, version `1.6-SNAPSHOT`):

- `./mvnw -Ppre-commit clean install` passes (exit 0, all tests green).
- However, the pre-commit linter/rewrite recipes leave the working tree **dirty**: 32 files
  modified (formatting, import order, and a `maven.compiler.release` 17 → 21 bump). See A6.

Finding IDs are stable and referenced by the PR clusters in the [Fix Plan](#3-fix-plan).
Severity: **[C]** critical, **[M]** major, **[m]** minor.

---

## 1. Findings

### A. Build, dependencies & tooling

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| A1 | M | `pom.xml:70-74` | Compile dependency on `mockwebserver3-junit5`, but main code only imports `mockwebserver3.*` core classes. The `-junit5` artifact contributes only OkHttp's own `StartStop` extension (unused) and drags `junit-jupiter-api` along; the actually-used `mockwebserver3` artifact is only an undeclared transitive dependency. | Depend on `com.squareup.okhttp3:mockwebserver3` (compile). Drop `-junit5` entirely (tests don't use it either). |
| A2 | m | `pom.xml:38-59` vs `60-92` | `dependencyManagement` declares scopes (`test`, `provided`) that the `<dependencies>` section partially overrides — misleading for a single-module POM. | Keep only versions in `dependencyManagement`; declare all scopes explicitly in `<dependencies>` (e.g. `jakarta.servlet-api` → explicit `provided`). |
| A3 | m | `CombinedDispatcher.java:27`, `DispatcherResolverTest.java:23`, others | `org.jetbrains.annotations.NotNull` used without a declared dependency (arrives transitively via `kotlin-stdlib`); inconsistent with the project's Lombok `@NonNull` convention. | Replace with `lombok.NonNull` everywhere. |
| A4 | m | `pom.xml:95-98` | Explicit test-scoped `junit-jupiter-params` is redundant — the `junit-jupiter` aggregate (from parent BOM) already includes it. | Remove the entry. |
| A5 | m | `pom.xml:89-92` | `jakarta.servlet-api` scope (`provided`) is inherited invisibly from `dependencyManagement`. | Declare `<scope>provided</scope>` at the dependency. |
| A6 | M | whole tree | Running the documented pre-commit build (`./mvnw -Ppre-commit clean install`) modifies 32 files — the repo has drifted from its own linter. Includes a `maven.compiler.release` 17 → 21 bump from the cui-rewrite recipes. | Run the pre-commit build once, review, and commit the drift. See [Decision D-1](#2-open-decisions) for the 17 → 21 bump. |
| A7 | m | `.github/dependabot.yml` | No `package-ecosystem: github-actions` entry; SHA-pinned `cuioss/cuioss-organization` reusables only advance via org-driven chore PRs. | Add the ecosystem block — or explicitly leave as-is if org automation is the intended sole updater (git history suggests it is; then close as "won't fix"). |
| A8 | M | many main files | OpenRewrite scanner markers (`/*~~(TODO: Catch specific not Exception ... )~~>*/`, `/*~~(TODO: INFO needs LogRecord ...)~~>*/`) are committed into production sources: `MockWebServerExtension` (8×), `DispatcherResolver` (3×), `CertificateResolver` (4×), `URIBuilder:99`, `KeyMaterialUtil` (2×), `MockResponseConfigResolver` (4×), `CombinedDispatcher` (3×). They flag a real smell: pervasive `catch (Exception)` and INFO-level logging. | Each fix PR that touches a file resolves the underlying issue (narrow the catch or add the documented `// cui-rewrite:disable` suppression) and strips the marker. PR-1 sweeps any file no other cluster touches. |
| A9 | m | misc | Cosmetics: `HttpMethodMapper` GET enum body mis-indented; `MockWebServerExtension.java:442` declares `parameterResolvers` field mid-class between methods; `lombok.config` lacks trailing newline. | Fix alongside A6 lint commit. |

### B. Extension lifecycle & HTTPS (`MockWebServerExtension`, `CertificateResolver`)

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| B1 | **C** | `MockWebServerExtension.java:190-196` | `beforeEach` swallows every exception that is not `IllegalStateException`/`DispatcherResolutionException`: it logs and returns normally, the `finally` block closes the never-stored server, and the test then fails later with the misleading `ParameterResolutionException: No MockWebServer instance available`, hiding the root cause. | Rethrow all setup failures (wrap in `IllegalStateException` or JUnit's `ExtensionConfigurationException`). Never continue silently. |
| B2 | M | `MockWebServerExtension.java:403-423` + `251-258` | `extractClassHierarchy` adds **enclosing** classes before the test class, and `findEnableMockWebServerAnnotation` takes `findFirst()` — so an outer `@EnableMockWebServer` **overrides** a `@Nested` class's own annotation (most-specific config cannot win). Only one enclosing level is walked (`getEnclosingClass()` once), so doubly-nested classes never see the outermost annotation. Note: `MockWebServerExtensionErrorHandlingTest.NestedWithDifferentConfig` currently pins this surprising behavior. | Reverse the search order (test class → superclasses → enclosing chain, most-specific first) and loop the enclosing walk. Behavioral change: update the pinning test and add a migration note. See [Decision D-2](#2-open-decisions). |
| B3 | M | `MockWebServerExtension.java:499-515`, `URIBuilder` | With `manualStart=true`, the injected `URIBuilder` is a placeholder whose comment claims it "will be updated when the server is actually started" — nothing ever updates it; `build()` unconditionally throws `IllegalStateException`. The injected parameter is unusable by design. README lines 205-220 even showcase the broken pattern. | Bind the placeholder lazily to the stored `MockWebServer` (resolve host/port at `build()` time), or fail fast at parameter resolution and document that `URIBuilder` injection is unsupported with `manualStart`. Lazy binding recommended. |
| B4 | m | `MockWebServerExtension.java:369` vs `383` | Store key mismatch: `put` uses key `MockWebServer.class.getName()`, `remove` uses `context.getRequiredTestMethod()` — the remove is a no-op yet logs "Removed MockWebServer from context". | Use one key constant in both (or drop `remove()`; the method-scoped store dies with the context anyway). |
| B5 | m | `MockWebServerExtension.java:232-243` | `ensureServerNotStarted` is dead code — a freshly constructed `MockWebServer` can never be started at that point. | Delete the check. |
| B6 | M | `CertificateResolver.java:253-284` | `SSLContext` is stored in the **root** extension-context store under a fixed key and never removed. (1) A test class *without* `useHttps` that injects `SSLContext` silently receives a stale context from a previously-run HTTPS class instead of the documented `ParameterResolutionException`. (2) Under parallel class execution, classes with different certs race on the same key → nondeterministic handshake failures. | Store the `SSLContext` in the class-level context store. Keep only the self-signed certificate *cache* at root. |
| B7 | m | `CertificateResolver.java:66-72` | `@TestProvidedCertificate` lookup uses raw `getAnnotation`: no superclass walk, no enclosing-class search for `@Nested` tests, no meta-annotations — inconsistent with the `AnnotationSupport`-based lookups used elsewhere. | Use `AnnotationSupport.findAnnotation(..., SearchOption.INCLUDE_ENCLOSING_CLASSES)`. |
| B8 | m | `CertificateResolver.java:106-109` | If the user-specified `methodName()` is not found, the code silently falls back to the default method name, then to self-signed certs — a typo silently changes the certificate source. | Fail fast (throw) when an explicitly configured method name doesn't resolve. |
| B9 | m | `TestProvidedCertificate.java:100` vs `CertificateResolver.java:44` | Two competing "default" provider-method names: annotation default `getTestProvidedHandshakeCertificates` vs resolver fallback `provideHandshakeCertificates` (which the annotation javadoc also calls "the default"). Hard to reason about; caused real dead test fixtures (H2). | Unify on one default name, keep the other as documented legacy fallback, and document the lookup order in the annotation javadoc. |

### C. Dispatcher resolution & dispatch (`DispatcherResolver`, `CombinedDispatcher`, `HttpMethodMapper`)

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| C1 | **C** | `DispatcherResolver.java:241-245, 262-267, 281-285` | `resolveFromAnnotation` catches **all** exceptions (missing no-arg constructor, missing provider method, provider throws) and returns `Optional.empty()`; `resolveDispatcher` then silently falls back to the default `/api` `CombinedDispatcher`. Proof in-repo: tests use `@ModuleDispatcher(provider = BaseAllAcceptDispatcher.class, providerMethod = "getOptimisticAPIDispatcher")` — `BaseAllAcceptDispatcher` has no no-arg constructor, so resolution always fails and those tests only pass because the fallback happens to be an identical `/api` dispatcher. A user with a non-`/api` base URL gets unexplained 418s. | When an explicitly configured `@ModuleDispatcher` cannot be resolved, throw `DispatcherResolutionException` instead of falling back. |
| C2 | **C** | `DispatcherResolver.java:149, 436-449` | `resolveDirectDispatcher` looks the provider method up on the **test class** (`testClass.getDeclaredMethod`) instead of `annotation.provider()`, and invokes it with `invoke(null)` assuming static. Any direct-`Dispatcher` provider on an external class silently resolves nothing (exceptions swallowed at debug), and the test silently runs against the `/api` fallback. | Look the method up on `annotation.provider()`; support instance methods the same way `resolveFromAnnotation` does. |
| C3 | M | `DispatcherResolver.java:102-106` | A provider method returning a raw `Dispatcher` short-circuits the whole resolution, skipping `@MockResponseConfig` annotations and `getModuleDispatcher()` — contradicting the documented priority order (CLAUDE.md: `@MockResponseConfig` first). | Only short-circuit when no other sources exist, or document the direct-`Dispatcher` case as exclusive-by-design and validate/fail loudly when combined with other sources. |
| C4 | M | `DispatcherResolver.java:402-426` | Method-level `@MockResponseConfig` for the same path+method as a class-level one triggers the conflict `IllegalStateException` — the natural "override for this test" use case fails in `beforeEach` instead of overriding. | De-duplicate by precedence (method > class > superclass) before validating; only same-level duplicates are conflicts. |
| C5 | M | `DispatcherResolver.java:314` | `getDeclaredMethod("getModuleDispatcher")` only finds the method declared on the concrete test class; inherited from an abstract base test class (common pattern) → silently not found → `/api` fallback. | Walk the superclass hierarchy (`ReflectionUtils.findMethod`). |
| C6 | m | `DispatcherResolver.java:128-158, 168-177, 271-276` | Confusing double-resolution flow: `resolveFromAnnotationSource` resolves the annotation, discards the result in most paths, then `collectDispatchers` re-resolves the same annotation — dispatchers get instantiated twice per test; the `instanceof Dispatcher` branch at 271-276 defers to handling that never happens. | Resolve each annotation exactly once and pass the result through. |
| C7 | M | `HttpMethodMapper.java:121-123` + `CombinedDispatcher.java:109` | `HttpMethodMapper.of()` uses `valueOf(...)` — any request with OPTIONS/PATCH/TRACE/CONNECT (e.g. a CORS preflight) throws `IllegalArgumentException` out of `dispatch()`, aborting the connection instead of the documented 418/404 fallback. | Make `of()` total (return `Optional`/`UNKNOWN`) and have `dispatch()` answer unknown methods with the teapot/404 response. |
| C8 | m | `CombinedDispatcher.java:112-124` | Leftover debug block: for every request, every non-matching dispatcher logs its bare base URL at INFO — and the URL is passed as the CuiLogger *format string* (a `%` in it corrupts formatting); `filtered.contains()` is O(n) in the loop. | Delete the block (or a single `debug` with proper template). |
| C9 | m | `CombinedDispatcher.java:117` | Pure `startsWith` prefix matching: base URL `/api` also captures `/apiary/...`. | Match on segment boundary (`equals(base) \|\| startsWith(base + "/")`) — or document the pitfall. |
| C10 | m | `CombinedDispatcher.java:78-80` | Raw status literals `404`/`418` instead of the project convention `HttpServletResponse.SC_NOT_FOUND` (418 has no constant — keep literal with comment). Constants also lack javadoc. | Use `SC_NOT_FOUND`; add javadoc. |
| C11 | m | `BaseAllAcceptDispatcher.java:186-189` | `getOptimisticAPIDispatcher()` is an *instance* method returning a fresh hardcoded `/api` dispatcher — unusable as a `providerMethod` target (its class has no no-arg constructor); the `@SuppressWarnings("unused") // Implicitly called by the test framework` claim is false today. | Make it `static` (this alone makes the existing annotations in tests/docs actually work), or remove it in favor of `CombinedDispatcher.createAPIDispatcher()`. |
| C12 | m | `ModuleDispatcher.java:80` + cross-cutting | `@Target` includes `METHOD` but the resolver only looks at the class — method-level `@ModuleDispatcher` compiles and is silently ignored (README even shows one). Annotation lookup strategy differs per annotation type (`@EnableMockWebServer`: enclosing-first custom walk; `@ModuleDispatcher`: class only; `@MockResponseConfig`: full manual walk; `@TestProvidedCertificate`: declared-only). | Implement method-level `@ModuleDispatcher` resolution (preferred, it's documented in README) or drop `METHOD` from the target. Standardize all lookups on `AnnotationSupport` with enclosing-class search. |

### D. `@MockResponseConfig` subsystem (`mockresponse/`)

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| D1 | M | `MockResponseConfigDispatcherElement.java:79-122` | Implements `handleGet/Post/Put/Delete` but **not `handleHead`**, while `method = HttpMethodMapper.HEAD` is legal and `supportedMethods()` reports it — HEAD requests fall through to the 418 default. | Add the `handleHead` override (HEAD responses: headers/status without body). |
| D2 | m | `MockResponseConfigDispatcherElement.java:260-278` | `convertKeyValuePairsToJson` splits naively on `,`/`=` and never escapes `"`/`\` — `message=Hello, world` silently produces corrupt/truncated JSON. | Escape values, split only on top-level separators; document the format limits in `MockResponseConfig.jsonContentKeyValue` javadoc. |
| D3 | m | `MockResponseConfigDispatcherElement.java:154-158` | `headers()` entries without `=` are silently ignored — a typo (`"ContentType: text/html"`) yields no header, no warning. | Throw (or at least warn) on malformed header entries. |
| D4 | m | `MockResponseConfigResolver.java:79-96` | Legacy mode (`testMethod == null`, still public API) aggregates annotations from **all** nested classes and **all** methods (sibling mocks of the same path+method blow up in validation) and does **not** walk superclasses — context-aware mode does. The two modes see different annotation sets for the same class. | Align the two collection strategies (reuse the context-aware walk; for legacy mode, only class-level annotations). |
| D5 | m | `MockResponseConfigResolver.java:173, 192` | `LOGGER.error("...%s", e.getMessage())` without the throwable-first argument — stack trace lost (line 112 does it correctly). | `LOGGER.error(e, "...")`. |

### E. SSL utilities (`ssl/KeyMaterialUtil`)

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| E1 | M | `KeyMaterialUtil.java:82-107` | `createSelfSignedHandshakeCertificates(durationDays, keyAlgorithm)` **ignores `keyAlgorithm`** — hardcoded `.rsa2048()`. Callers requesting EC silently get RSA; `doc/HttpsSupport.adoc` advertises the parameter. | Map `KeyAlgorithm` → `HeldCertificate.Builder` (`ecdsa256()`/`rsa2048()`). |
| E2 | M | `KeyMaterialUtil.java:119-165` | `convertToHandshakeCertificates(SSLContext)` converts nothing: it fabricates a brand-new self-signed certificate + system trust roots, unrelated to the input context (the comment admits it). Javadoc promises the opposite. Unused by main code. | Deprecate + rewrite javadoc to state actual behavior ("generates fresh self-signed material"); remove in the next major. |
| E3 | m | `KeyMaterialUtil.java:183-189` | `SecureRandom.getInstance("SHA1PRNG")` + `setSeed(System.currentTimeMillis())` **replaces** the PRNG state with a low-entropy timestamp (for SHA1PRNG, pre-use `setSeed` is the sole seed) while the comment claims "properly seeded". Unnecessary: `SSLContext.init` accepts `null`. | Pass `null` (or `new SecureRandom()`). |

### F. `URIBuilder`

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| F1 | M | `URIBuilder.java:211-242` | No percent-encoding anywhere: `addQueryParameter("q", "a b")` → `URI.create` throws `IllegalArgumentException`; values containing `&`/`=`/`#` silently produce a structurally different URI. | Encode query names/values with `URLEncoder.encode(..., UTF_8)` and path segments per RFC 3986 (or delegate to `okhttp3.HttpUrl.Builder`, already on the classpath). Document. See [Decision D-3](#2-open-decisions). |
| F2 | m | `URIBuilder.java:189-203` | `validateBuilderState` duplicates the placeholder message in two branches whose only difference is a typo'd code sample with unbalanced parentheses (`server.url('/')).url())`). Tests assert these exact broken strings (H12). | One message constant, correct sample; relax test assertions to `contains(...)`. |
| F3 | m | `URIBuilder.java:200-202, 265-268, 280-283, 295-298` | Four `baseUrl == null` branches are unreachable (both constructors guarantee `placeholder ⇔ baseUrl == null`); tests reach them only by reflection-nulling a `private final` field (H10). | Remove the dead branches and the reflection tests. |
| F4 | m | `URIBuilder.java:334-340` | `setPath("/api/v2/users")` stores the whole path as a *single* segment (embedded slashes) and doesn't touch query params — `getPathSegments()` reports `["api/v2/users"]`, inconsistent with `addPathSegment` semantics. | Split the incoming path on `/` into segments. |

### G. Documentation (README, `doc/`, javadoc)

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| G1 | M | `README.adoc:91-98` | Example calls `new BaseAllAcceptDispatcher("/api/posts").withPostResponse(...)` — `withPostResponse` does not exist. | Use `dispatcher.getPostResult().setResponse(...)` or `setMethodToResult(...)`. |
| G2 | M | `README.adoc:218`, `EnableMockWebServer.java:56` | `server.shutdown()` does not exist in mockwebserver3 5.x (renamed `close()`). | `server.close()` — and note the extension closes the server automatically in `afterEach`. |
| G3 | M | `README.adoc:205-220` | Manual-start example builds a URI from the *injected* `URIBuilder` — the placeholder whose `build()` always throws (B3); the very next README section calls the same pattern "INCORRECT". | Rewrite once B3 is decided (lazy binding → example becomes valid; fail-fast → use `URIBuilder.from(server.url("/").url())`). |
| G4 | m | `README.adoc:30-36, 84-90` | Maven snippet has no `<version>`; method-level `@ModuleDispatcher` comment claims behavior that doesn't exist (C12). | Add version placeholder/note; fix or remove the method-level example (aligned with C12's resolution). |
| G5 | M | `doc/MockResponse.adoc:1, 57-74, 93` | All three "Content Types" examples omit the **required** `status` attribute (no default — they don't compile); title says `@MockResponse` instead of `@MockResponseConfig`; feature attributed to "version 1.2" here vs "1.1" in Migration.adoc. | Add `status = 200`; fix title; pick one version. |
| G6 | M | `doc/ModuleDispatcher.adoc:89, 107, 309, 356-363, 391-427` | Examples use nonexistent `request.getPath()` (3×); `JwksResolveDispatcher` example references undefined constant `PUBLIC_KEY_JWKS`; "Implementation example" never registers the dispatcher it asserts on (call counter would stay 0). | `request.getUrl().encodedPath()`; define the constant; register the dispatcher via `getModuleDispatcher()`. |
| G7 | M | `doc/Migration.adoc:88-117, 265-270` | Snippet declares `@ModuleDispatcher` **twice** on one class (not `@Repeatable` — compile error) and is missing a closing brace; `headers = "Content-Type=application/json;ETag=W/123"` implies a `;`-multi-header syntax that doesn't exist (produces one header with a `;` in the value). | Fix the snippet; `headers = {"Content-Type=application/json", "ETag=W/123"}`. |
| G8 | m | `doc/HttpsSupport.adoc:23, 82-86, 145-151` | Shows the `getOptimisticAPIDispatcher` provider pattern that only works via the silent fallback (C1/C11); advertises the ignored `keyAlgorithm` parameter (E1). | Update after C11/E1 land. |
| G9 | M | `MockWebServerExtension.java:104-107`, `EnableMockWebServer.java:40`, `ModuleDispatcherElement.java:47-51`, `BaseAllAcceptDispatcher.java:50-52` | Javadoc examples use the removed `okhttp3.mockwebserver` API (`new MockResponse().setBody(...)`, `.setResponseCode(...)`, `request.getPath()`) — none compile against mockwebserver3, contradicting CLAUDE.md conventions. | Rewrite with `MockResponse.Builder` and `request.getUrl().encodedPath()`. |
| G10 | M | `EnableMockWebServer.java:119-127` | Claims parameter injection of "port" and "URL" — the extension only resolves `MockWebServer`, `URIBuilder`, `SSLContext`. | Correct the list. |
| G11 | M | `BaseAllAcceptDispatcher.java:58-65`, `EndpointAnswerHandler.java:61-62` | Documented defaults are wrong: docs promise POST → 201 Created, PUT → 204 No Content; code gives POST → 200 OK, PUT → 201 Created (tests pin the code's behavior; `doc/ModuleDispatcher.adoc:63` documents it correctly). | Align javadoc to actual behavior (non-breaking). See [Decision D-4](#2-open-decisions). |
| G12 | m | `EndpointAnswerHandler.java:52-56, 140-149, 291-297` | Javadoc example uses nonexistent `EndpointAnswerHandler.builder()`; constructor has empty `@param` tags; factory `noContent(...)` actually produces a *no-response/fall-through* handler — semantically collides with HTTP 204 "No Content". | Fix example + params now; rename `noContent` → `unhandled`/`passthrough` deferred to next major (add clarifying javadoc now). |
| G13 | m | `CombinedDispatcher.java:47-50`, `ModuleDispatcherElement.java:77`, `HttpMethodMapper.java:33` | Example calls nonexistent `.setEndWithTeapot(false)` (real: `endWithTeapot`); two javadocs list supported methods as "GET, POST, PUT, DELETE" omitting HEAD. | Fix names; add HEAD. |
| G14 | m | `package-info.java:35,81,86`; `ssl/`, `mockresponse/` | `{@link}`s to the `ssl` package cannot resolve — `ssl` and `mockresponse` have **no `package-info.java`**; `mockresponse` missing from the package overview. | Add both package-infos; extend the overview. |

### H. Test suite

| ID | Sev | Location | Finding | Fix hint |
|----|-----|----------|---------|----------|
| H1 | M | `MockWebServerErrorHandlingTest.java:48-72` | `shouldHandleParameterResolutionErrors` asserts literally `assertTrue(true)`; its `MockExtensionWithoutServer` fixture is never invoked (test method has no parameters). The real error path stays untested while appearing covered. | Delete test + fixture; write a real test asserting `ParameterResolutionException` (e.g. via JUnit `EngineTestKit`). |
| H2 | M | `MockWebServerExtensionCertificateTest.java:241-244, 301-305`, `MockWebServerExtensionErrorHandlingTest.java:239-243` | Provider methods named `provideCertificates` match **neither** default name (B9) and are never called — "Should use certificates provided by test class" actually runs on self-signed fallback certs; three tests verify nothing they claim; the provider-returns-null branch is uncovered. | Rename to the resolved default (or set `methodName`), assert the provider was actually used (compare trust material), cf. the correct pattern in `https/HttpsTests:154-201`. |
| H3 | M | `MockWebServerManualStartTest.java:97-123` | `shouldStartServerOnSpecificPort` uses fixed port 8090; on `IOException` it only logs — flaky by environment and vacuous in its failure mode. | Drop the fixed-port test, or reserve a free port via `new ServerSocket(0)` first and fail on `IOException`. |
| H4 | M | `dispatcher/PathBasedDispatcherTest.java:100-151` | The injected `URIBuilder` accumulates path segments across four requests (second request really hits `/api/users/api/users/active`); assertions pass only because the dispatcher matches with `endsWith`/regex. Documents the README pattern — would mask real routing regressions and teaches broken usage. | Use `setPath(...)` per request or a fresh builder per request; tighten the dispatcher match. |
| H5 | m | `MockWebServerExtensionErrorHandlingTest.java:294-328` | `shouldHandleServerPortConflicts` wraps its body in `catch (Exception) { log }` — any infrastructure failure silently passes; name says "extension" but tests `MockWebServer` itself. | Remove the outer catch; keep the `assertThrows`. |
| H6 | m | multiple | Heavy duplication between test classes: `MockWebServerErrorHandlingTest` vs `MockWebServerExtensionErrorHandlingTest`; manual start covered twice; certificate fallback twice; SSL-handshake failure twice; parameter injection three times. Slows the build (each HTTPS duplicate regenerates certs). | Fold `MockWebServerErrorHandlingTest` into `MockWebServerExtensionErrorHandlingTest`; keep one canonical test per behavior. |
| H7 | m | `DispatcherResolverTest:226-271`, `DispatcherResolverMethodTest:123-162`, `DispatcherResolverAdvancedTest:343-367`, + 3× `RecordedRequest` factories, + 2× body-readers | `TestDispatcherElement` copy-pasted three times; hand-built 13-arg `RecordedRequest` factory three times; body-to-string helper twice. | One package-private test-fixture class per concern. |
| H8 | m | `CertificateResolverTestUtil.java:31-32`, `CertificateResolverTest.java:364-368` | Test util re-declares `CertificateResolver`'s **private** store keys; EasyMock scripts exact store choreography — tests "how", not "what". | Expose the keys as package-visible constants, or assert observable behavior (cache reuse) only. |
| H9 | m | `DispatcherResolverAdvancedTest.java:43-101, 119-161, 196-209` | Many tests assert only `assertInstanceOf(CombinedDispatcher.class, ...)` — true for both "resolved" and "fell back to default" (cannot detect C1-class bugs); `shouldHandlePublicMethodNotAccessible` claims a scenario its fixture doesn't create. | Dispatch a probe request and assert the expected status/body, distinguishing custom responses from the 418/`/api` default. Rework alongside C1/C2 (fallback→throw changes expectations). |
| H10 | m | `URIBuilderExceptionTest.java:56-111`, `URIBuilderTestBase.java:71-83` | Reflection-nulls a `private final` field to reach unreachable `baseUrl == null` branches (F3). | Delete with F3. |
| H11 | m | `URIBuilderSpecialCharacterTest.java:24-39` | "SpecialCharacter" test contains no special characters; only pre-encoded values used elsewhere; raw-unencoded input (space, `&`) is untested — exactly where F1 bites. | Add real special-character tests pinning the post-F1 encoding behavior; rename class. |
| H12 | m | `URIBuilderPlaceholderTest.java:42-68` | Asserts two full exact message strings that differ only by the typo'd parenthesis (F2) — locks the typo in. | Assert with `contains(...)`; update with F2. |
| H13 | m | `dispatcher/BaseAllAcceptDispatcherTest.java:40-50, 84-94` | `shouldModifyMethodsWithNull` and `shouldModifyOtherMethodsToForbidden` end up modifying nothing and re-assert defaults; the interesting proper-subset case is never tested. | Add a subset test (forbid only GET → GET 403, POST still default); rename the vacuous ones. |
| H14 | m | coverage gaps | Untested main-code paths: `SSLContext` injection without `useHttps` (expected `ParameterResolutionException`); unsupported HTTP methods through `dispatch()` (C7); placeholder `URIBuilder` injection under `manualStart` (B3); `beforeEach` failure behavior (B1); `CertificateResolver` provider-method-throws path. | Add targeted tests **in the same PRs as the corresponding fixes** (they become the regression tests). |

---

## 2. Open decisions

Recommendations included — fix sessions should follow these unless the maintainer overrides.

| ID | Decision | Recommendation |
|----|----------|----------------|
| D-1 | `maven.compiler.release` 17 → 21 (forced by the parent's cui-rewrite recipe, A6) | **Accept 21.** The org tooling actively rewrites to 21; pinning 17 would fight the parent on every build. Flag in release notes: consumers need JRE 21+. If 17 support is still required, the recipe must be suppressed in the parent instead — escalate to the maintainer in the PR description. |
| D-2 | Nested-class annotation precedence (B2): keep "outer wins" (current, pinned by a test) or switch to "most-specific wins"? | **Most-specific wins.** It matches JUnit conventions and user expectations (`@Nested` with its own `@EnableMockWebServer(useHttps=true)` must be honored). Update the pinning test, note in Migration.adoc. |
| D-3 | `URIBuilder` encoding (F1): start percent-encoding (behavior change) or only document raw behavior? | **Encode.** Current behavior throws or silently corrupts — nobody can depend on it usefully. Encode query names/values and path segments; document in javadoc + note in Migration.adoc. |
| D-4 | POST/PUT default status codes (G11): change code to match docs (breaking) or docs to match code? | **Docs to code.** Existing tests pin POST→200/PUT→201; changing runtime behavior of a published test library breaks consumers' assertions. Fix the three wrong javadoc locations. |
| D-5 | `convertToHandshakeCertificates(SSLContext)` (E2): fix, re-document, or remove? | **Deprecate + re-document now** (it cannot be implemented as promised — key material is not extractable from an `SSLContext`); remove in the next major. |
| D-6 | `EndpointAnswerHandler.noContent()` naming (G12): rename now? | **Defer rename to next major** (breaking); add clarifying javadoc now. |

---

## 3. Fix Plan

### Ground rules (every PR)

Per repository policy (CLAUDE.md) and this plan's mandate, **each PR cluster follows the same
end-to-end workflow**:

1. `git checkout main && git pull`, then create the feature branch listed below.
2. Implement **only** the finding IDs in scope for the cluster. Strip OpenRewrite markers (A8)
   in every touched file as part of the fix.
3. Add/adjust tests in the same PR — every behavior fix ships with the regression test that
   would have caught it (H14 mapping).
4. Verify locally: `./mvnw -Ppre-commit clean install` must pass **and leave the working tree
   clean** (after PR-1 establishes the clean baseline).
5. Push, create the PR against `main` (title prefix `fix:`/`chore:`/`docs:` as appropriate;
   reference the finding IDs from this document in the PR body).
6. **Wait ~5 minutes** for CI and the Gemini review to arrive (`gh pr checks --watch`).
7. Handle **every** review comment: fix + reply, or reply with justification; **resolve every
   thread**. If a comment's validity is uncertain, ask the maintainer before acting.
8. Merge once CI is green and all threads are resolved (merging is explicitly authorized for
   this plan). Do **not** enable auto-merge.
9. `git checkout main && git pull` before starting the next cluster.

Clusters are ordered so later PRs build on earlier ones (lint baseline first, behavior before
docs). PR-2 … PR-5 are mutually independent and may be reordered if needed; PR-6 and PR-7
must come last (they depend on the behavior decisions landing first).

### PR-1 — `fix/build-and-lint-baseline` (chore)

**Scope:** A1–A7, A9, remaining A8 markers in files no later cluster touches.

- Apply the pre-commit lint/rewrite drift as its own commit (A6, includes D-1 → release 21).
- pom.xml: swap `mockwebserver3-junit5` → `mockwebserver3` (A1); clean `dependencyManagement`
  scopes (A2, A5); drop `junit-jupiter-params` (A4).
- Replace `org.jetbrains.annotations.NotNull` with `lombok.NonNull` (A3).
- dependabot github-actions ecosystem (A7) — or record "won't fix, org automation owns it".
- Risk note: A1 changes the published dependency graph — verify `mockwebserver3` lands as a
  direct compile dependency in the generated POM (`mvn dependency:tree`).

### PR-2 — `fix/dispatcher-resolution` (fix)

**Scope:** C1–C6, C11, C12 · related docs deferred to PR-7 · tests: H9 rework.

- Fail loudly instead of silent `/api` fallback (C1) — this flips expectations in
  `DispatcherResolverAdvancedTest`; rework those tests to probe-request assertions (H9).
- Fix provider lookup class + instance-method support (C2); single-pass resolution (C6).
- Direct-`Dispatcher` combination semantics (C3); method>class override precedence (C4);
  inherited `getModuleDispatcher()` (C5).
- Make `getOptimisticAPIDispatcher()` static (C11) — existing annotations start *actually*
  resolving; confirm affected tests still pass for the right reason.
- Method-level `@ModuleDispatcher` + unified annotation lookup (C12).

### PR-3 — `fix/dispatch-and-mockresponse` (fix)

**Scope:** C7–C10, D1–D5 · tests: H14 (unsupported-methods, HEAD).

- Total `HttpMethodMapper.of()` + 418/404 for unknown methods (C7) with regression test.
- Remove leftover INFO block / format-string misuse (C8); segment-boundary matching (C9);
  status constants + javadoc (C10).
- `handleHead` support (D1); JSON escaping + documented limits (D2); malformed-header
  handling (D3); align legacy/context-aware resolution modes (D4); error-logging with
  throwable (D5).

### PR-4 — `fix/extension-lifecycle-and-https` (fix)

**Scope:** B1–B9, E1–E3 · tests: H14 (beforeEach failure, SSLContext-without-HTTPS,
placeholder injection, provider-throws), H2 prerequisite (B9 decides the provider-name rule).

- Rethrow setup failures (B1); most-specific-wins annotation search incl. full enclosing walk
  (B2, per D-2 — update the pinning test); lazy-bound `URIBuilder` for `manualStart` (B3).
- Store-key fix / remove no-op (B4); delete dead `ensureServerNotStarted` (B5).
- Class-level `SSLContext` store (B6); `AnnotationSupport`-based `@TestProvidedCertificate`
  lookup (B7); fail-fast on missing named provider method (B8); unify default provider-method
  names + document lookup order (B9).
- `KeyMaterialUtil`: honor `keyAlgorithm` (E1); deprecate + re-document
  `convertToHandshakeCertificates` (E2, per D-5); fix `SecureRandom` seeding (E3).
- Demote routine INFO logging to debug across extension/resolver/dispatcher (A8 follow-through).

### PR-5 — `fix/uribuilder-encoding` (fix)

**Scope:** F1–F4 · tests: H10–H12.

- Percent-encoding for query params + path segments (F1, per D-3) with real
  special-character tests (H11).
- Single validation message, typo fixed (F2) + `contains`-based assertions (H12).
- Remove unreachable null branches + reflection tests (F3/H10); `setPath` segment splitting (F4).
- Note in Migration.adoc (coordinated with PR-7 if needed — a one-line stub here is fine).

### PR-6 — `fix/test-suite-quality` (test)

**Scope:** H1–H8, H13 (H9–H12, H14 already land with PR-2…PR-5).

- Delete/replace vacuous tests (H1); fix dead certificate-provider fixtures (H2 — after PR-4's
  B9 rule); fix or drop the fixed-port test (H3); per-request path reset in
  `PathBasedDispatcherTest` (H4); remove exception-swallowing wrapper (H5).
- Consolidate duplicate test classes (H6) and shared fixtures (H7); stop testing private store
  keys (H8); `BaseAllAcceptDispatcher` subset test (H13).

### PR-7 — `docs/align-docs-with-code` (docs)

**Scope:** G1–G14 (G11 per D-4, G3 per B3 outcome, G8 after C11/E1) — README, all four
`doc/*.adoc` guides, all javadoc examples, both missing `package-info.java` files.

- Every code example must compile against mockwebserver3 5.x conventions
  (`MockResponse.Builder`, `server.close()`, `request.getUrl().encodedPath()`).
- Must run last: examples document the *fixed* behavior of PR-2…PR-5.

### Final Verification & Document Removal

After PR-7 is merged, run a dedicated verification pass **before** touching this document:

1. `git checkout main && git pull`; run `./mvnw -Ppre-commit clean install` — must pass with a
   clean working tree.
2. Walk **every finding ID** (A1…H14) in this document against the current code and record
   fixed / consciously-not-fixed-with-reason (decisions D-1…D-6 count as resolutions for the
   items they cover). Independent re-review of the touched areas is encouraged (fresh eyes,
   e.g. subagents re-reading the fixed files).
3. If any finding is unresolved without a recorded justification → fix it first (small
   follow-up PR into the matching cluster's scope).
4. When **all** findings are verified: create the final PR `chore/remove-review-26-07-04`
   that deletes `doc/review-26-07-04.md`, with the verification matrix (ID → resolution) in
   the PR body as the permanent record. Same PR workflow (CI + Gemini + resolve all comments),
   then merge.

---

*Analysis performed 2026-07-04 on commit `9f8cd77` by four parallel review passes
(production-code correctness, test suite, documentation accuracy, build/architecture),
cross-verified against the mockwebserver3 5.4.0 binary API and a full pre-commit build run.*
